{
  "name": "dynamics-365-power-pane-next",
  "description": "In-page action pane, diagnostics and real user impersonation for Microsoft Dynamics 365 / Dataverse.",
  "version": "1.2.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "check": "node scripts/check-syntax.js && node scripts/check-bridge-sync.js",
    "lint": "eslint .",
    "test": "node --test \"tests/**/*.test.js\"",
    "ci": "npm run check && npm run lint && npm run test"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.8.15",
    "eslint": "^9.39.5",
    "globals": "^16.5.0"
  }
}
